import { rpgGroups, rpgGroupTags, rpgMembers } from './rpg-group';
import { tags } from './tag';
import { refreshTokens, userInterests, users } from './user';

export const schema = {
  users,
  userInterests,
  tags,
  refreshTokens,
  rpgGroups,
  rpgGroupTags,
  rpgMembers,
};
